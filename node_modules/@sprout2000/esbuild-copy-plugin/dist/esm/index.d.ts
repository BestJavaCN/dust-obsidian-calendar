import fs from "node:fs";
import { Plugin } from "esbuild";
interface CopyPluginOptions extends fs.CopySyncOptions {
    src?: string;
    dest?: string;
}
export const getDigest: (string: string) => string;
export const getFileDigest: (path: string) => string | null;
export const filter: (src: string, dest: string) => boolean;
export const copyPlugin: (options?: CopyPluginOptions) => Plugin;
